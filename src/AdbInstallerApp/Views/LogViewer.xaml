<UserControl x:Class="AdbInstallerApp.Views.LogViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdbInstallerApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Application Logs" 
                   FontSize="16" FontWeight="Bold" Margin="10,5,10,5"/>
        
        <!-- Controls -->
        <Grid Grid.Row="1" Margin="10,5,10,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Filter Text -->
            <TextBox Grid.Column="0" 
                     Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,10,0"
                     VerticalAlignment="Center"
                     ToolTip="Filter logs by text content"/>
            
            <!-- Log Level Filter -->
            <ComboBox Grid.Column="1" 
                      SelectedItem="{Binding SelectedLogLevel}"
                      Margin="0,0,10,0"
                      Width="80"
                      VerticalAlignment="Center">
                <ComboBoxItem Content="All"/>
                <ComboBoxItem Content="Info"/>
                <ComboBoxItem Content="Warning"/>
                <ComboBoxItem Content="Error"/>
                <ComboBoxItem Content="Debug"/>
            </ComboBox>
            
            <!-- Show Debug Toggle -->
            <CheckBox Grid.Column="2" 
                      Content="Debug" 
                      IsChecked="{Binding ShowDebug}"
                      Margin="0,0,10,0"
                      VerticalAlignment="Center"/>
            
            <!-- Auto Scroll Toggle -->
            <CheckBox Grid.Column="3" 
                      Content="Auto Scroll" 
                      IsChecked="{Binding AutoScroll}"
                      Margin="0,0,10,0"
                      VerticalAlignment="Center"/>
            
            <!-- Clear Button -->
            <Button Grid.Column="4" 
                    Content="Clear" 
                    Command="{Binding ClearLogsCommand}"
                    Width="60"
                    VerticalAlignment="Center"/>
        </Grid>
        
        <!-- Log Entries -->
        <ScrollViewer Grid.Row="2" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding LogEntries}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="5,2" Padding="5" 
                                Background="{Binding Level, Converter={StaticResource LogLevelToBackgroundConverter}}"
                                BorderBrush="LightGray" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Column="0" 
                                           Text="{Binding TimeOnly}" 
                                           FontFamily="Consolas" 
                                           FontSize="10"
                                           Foreground="Gray"
                                           Margin="0,0,10,0"/>
                                
                                <TextBlock Grid.Column="1" 
                                           Text="{Binding Level}" 
                                           FontWeight="Bold"
                                           Margin="0,0,10,0"
                                           Foreground="{Binding Level, Converter={StaticResource LogLevelToForegroundConverter}}"/>
                                
                                <TextBlock Grid.Column="2" 
                                           Text="{Binding Message}" 
                                           TextWrapping="Wrap"
                                           FontFamily="Consolas"
                                           FontSize="11"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
        <!-- Status Bar -->
        <Grid Grid.Row="3" Background="LightGray" Margin="0,5,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" 
                       Text="{Binding LogEntries.Count, StringFormat='{}{0} log entries'}"
                       Margin="10,5"
                       VerticalAlignment="Center"/>
            
            <Button Grid.Column="1" 
                    Content="Copy All" 
                    Command="{Binding CopyToClipboardCommand}"
                    Margin="5"
                    Padding="10,5"
                    VerticalAlignment="Center"/>
            
            <Button Grid.Column="2" 
                    Content="Save to File" 
                    Command="{Binding SaveToFileCommand}"
                    Margin="5"
                    Padding="10,5"
                    VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</UserControl>
