<Window x:Class="AdbInstallerApp.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Height="180" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with icon -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="📦" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding Question}" 
                       FontSize="14" FontWeight="SemiBold" 
                       VerticalAlignment="Center"
                       Foreground="#333"/>
        </StackPanel>
        
        <!-- Input field -->
        <Border Grid.Row="1" Background="White" 
                BorderBrush="#DDD" BorderThickness="1" 
                CornerRadius="3" Margin="0,0,0,20">
            <TextBox Name="AnswerTextBox" 
                     Text="{Binding Answer, UpdateSourceTrigger=PropertyChanged}"
                     Padding="10" FontSize="13" 
                     BorderThickness="0" Background="Transparent"/>
        </Border>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Height="32" 
                    Margin="0,0,10,0" Click="Cancel_Click"
                    Background="#E0E0E0" BorderBrush="#CCC" 
                    Foreground="#666" FontWeight="SemiBold"/>
            <Button Content="Create Group" Width="100" Height="32" 
                    Click="OK_Click" IsDefault="True"
                    Background="#2196F3" BorderBrush="#1976D2" 
                    Foreground="White" FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</Window>
